# =============================================================================
# eluno.org - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# DOMAIN CONFIGURATION
# -----------------------------------------------------------------------------
DOMAIN=eluno.org
STATIC_SUBDOMAIN=static

# -----------------------------------------------------------------------------
# STATIC ASSETS SERVER (Hostinger)
# Used by: scripts/publish-media.js
# Purpose: Upload MP3/PDF files to static.eluno.org
# -----------------------------------------------------------------------------

# SSH/SFTP host for static assets
UPLOAD_HOST=your-server-ip

# SSH port (default: 22, Hostinger uses non-standard)
UPLOAD_PORT=22

# SSH username
UPLOAD_USER=your-username

# SSH password (consider using SSH keys instead for better security)
UPLOAD_PASS=your-password

# Remote directory path for static files
UPLOAD_DIR=/path/to/public_html/static

# -----------------------------------------------------------------------------
# OPTIONAL: API KEYS (for future use)
# -----------------------------------------------------------------------------

# Anthropic API (for AI translation)
# ANTHROPIC_API_KEY=sk-ant-...

# Fish Audio TTS API (for audiobook generation)
# FISH_AUDIO_API_KEY=...

# -----------------------------------------------------------------------------
# SEO
# -----------------------------------------------------------------------------

# Controls robots meta tag for search engine indexing.
# INDEXABLE=true  → <meta name="robots" content="index, follow">
# INDEXABLE=      → <meta name="robots" content="noindex, nofollow">
#
# HOW TO PUBLISH A SITE (when LL Research authorizes it):
# 1. Set INDEXABLE=true in the package's .env file
# 2. Rebuild: npm run build --workspace=@eluno/<package-name>
# 3. Deploy: npx wrangler pages deploy packages/<package-name>/dist --project-name=<project>
# 4. Verify: curl -s <site-url> | grep "robots"
#
# Current status:
#   eluno.org          → INDEXABLE=true  (authorized by LL Research)
#   todo.eluno.org     → INDEXABLE=      (pending authorization)
#   sanacion.eluno.org → INDEXABLE=      (pending authorization)
#   jesus.eluno.org    → INDEXABLE=      (pending authorization)
INDEXABLE=true

# -----------------------------------------------------------------------------
# ANALYTICS
# -----------------------------------------------------------------------------

# Google Analytics Measurement ID (leave empty to disable tracking)
GA_ID=G-XXXXXXXXXX
